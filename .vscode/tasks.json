{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean build - debug",
            "type": "shell",
            "command": "clear && cargo build",
            "options": {
                "cwd": "${workspaceFolder}",
            },
            "problemMatcher":[]
        }
    ]
}